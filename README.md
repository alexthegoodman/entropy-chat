# Entropy Chat

## Development

- `trunk serve --open`

Note: visiting `http://[::1]:8080/` will use live API, while `http://localhost:8080/` will use the local `entropy-site` api.

## Build

- `trunk build --release`