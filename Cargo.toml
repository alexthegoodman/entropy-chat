[package]
name = "entropy-chat"
version = "0.1.0"
edition = "2024"

[dependencies]
leptos = { version = "0.8.15", features = ["csr"] }
web-sys = { version = "0.3", features = ["Window", "Navigator"] }
